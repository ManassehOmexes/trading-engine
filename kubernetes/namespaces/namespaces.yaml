# Namespace fuer Daten-Ingestion (Alpaca WebSocket, Kafka Producer)
apiVersion: v1
kind: Namespace
metadata:
  name: data-ingestion
  labels:
    app.kubernetes.io/part-of: trading-engine
    environment: dev

---
# Namespace fuer ML Services (FinBERT, Indikator-Berechnungen)
apiVersion: v1
kind: Namespace
metadata:
  name: ml-services
  labels:
    app.kubernetes.io/part-of: trading-engine
    environment: dev

---
# Namespace fuer Trading Logic (Risk Manager, Order Executor)
apiVersion: v1
kind: Namespace
metadata:
  name: trading
  labels:
    app.kubernetes.io/part-of: trading-engine
    environment: dev

---
# Namespace fuer Kommunikation (Telegram Bot)
apiVersion: v1
kind: Namespace
metadata:
  name: notifications
  labels:
    app.kubernetes.io/part-of: trading-engine
    environment: dev

---
# Namespace fuer Monitoring (Prometheus, Grafana) - kommt in Phase 6
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    app.kubernetes.io/part-of: trading-engine
    environment: dev
